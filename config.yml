title: '<><img src=1 onerror=alert(document.domain)> '
description: '<><img src=1 onerror=alert(document.domain)> '
template:
  repo: lessgreaterlessimg-src1-onerroralert(document.domain)greater-template
  name: lessgreaterlessimg-src1-onerroralert(document.domain)greater
before:
  - type: createIssue
    title: Welcome!
    body: new-issue.md
steps:
  - title: First step
    description: The first step in your course
    event: issues.assigned
    link: '{{ repoUrl }}'
    actions:
      - type: closeIssue
